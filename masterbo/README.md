# masterbo

A small Python starter project with a command-line interface.

## Run

From the project directory:

```bash
python -m masterbo
```

Or install it in editable mode and use the command:

```bash
python -m pip install -e .
masterbo
```

## Development

The application code lives in `src/masterbo/`.